"""Optional Python-side architectural demo; production bindings can be added with pybind11."""
print("Python interoperability boundary ready.")
print("Build the C++ engine first, then expose Engine with pybind11 if desired.")
